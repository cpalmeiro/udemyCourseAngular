services:
  angular-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: angularApp
    ports:
      - "4200:4200"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
